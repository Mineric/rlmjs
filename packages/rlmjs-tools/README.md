# @software-machines/rlmjs-tools

Tool runtime helpers for `rlmjs`.

Provided:
- `createToolRegistry()` to build a typed `RlmToolRuntime`.
- `okToolResult()` / `errorToolResult()` helpers.

This package is meant to be paired with `@software-machines/rlmjs-core`.
